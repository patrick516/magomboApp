import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — deep navy (reverence, trust, stability)
  static const Color primary = Color(0xFF1B2A4A);
  static const Color primaryLight = Color(0xFF2E4270);

  // Accent — warm gold (worship, light, celebration)
  static const Color accent = Color(0xFFC9A24B);
  static const Color accentLight = Color(0xFFE0C378);

  // Neutrals
  static const Color background = Color(0xFFF9F7F2);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF6B6B6B);

  // Status
  static const Color success = Color(0xFF3C8D5C);
  static const Color error = Color(0xFFB3413A);
}